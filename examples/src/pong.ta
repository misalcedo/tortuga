@self = /pong

$message = i18n::pong
$unknown_sender = i18n::unknown_sender

accept (sender: @_, data: [5]) {
  sender ! (@self, $message)
}

accept (sender: @_, ...) {
  sender ! (@self, $unknown_sender)
}