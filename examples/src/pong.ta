@self = /pong

$message = urn::uuid:pong
$unknown_sender = urn::uuid:unknown_sender

accept (sender: @*, , data: [5]) {
  sender ! (@self, $message)
}

accept (sender: @*, ...) {
  sender ! (@self, $unknown_sender)
}