@self = /ping
@pong = /pong

$message = i18n::ping
$unknown_sender = i18n::unknown_sender

accept (@pong, data: [5]) {
  @pong ! (@self, $message)
}

accept (sender: @_, ...) {
  sender ! (@self, $unknown_sender)
}
