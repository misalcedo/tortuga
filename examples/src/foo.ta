@self = /ping
@pong = /pong

$message = urn::uuid:ping
$unknown_sender = urn::uuid:unknown_sender

accept (sender: /pong, x: 42, data: 5 bytes) {
  sender ! (@self, x, $message)
}

accept (sender: reference, ...) {
  sender ! (@self, $unknown_sender)
}
